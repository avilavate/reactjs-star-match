const helpers = {
    helper1: function(range){
        let rangeArray = [];
        for (let i = 0; i < range; i++) {
            rangeArray.push(i);
        }
        console.log(rangeArray);
        return rangeArray;
    },
   
};

export default helpers;